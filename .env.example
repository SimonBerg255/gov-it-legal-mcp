# No environment variables are required.
# This server runs without authentication by design.
# All configuration is handled through Railway environment settings.

# Optional: set PORT if running locally (Railway sets this automatically)
# PORT=8000
